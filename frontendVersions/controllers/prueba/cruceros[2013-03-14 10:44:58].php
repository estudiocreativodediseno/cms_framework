<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

class <!-- Etiqueta section_name no definida --> extends CI_Controller {			

    <!-- Etiqueta global_vars no definida -->
    
    function __construct(){
        parent::__construct();
        
        $this->load->database();
        $this->load->helper('url'); 
        $this->title = "<!-- Etiqueta title_page no definida -->";
    }
    

  	/**
     * Código generado automáticamente por CMS-FRAMEWORK [<!-- Etiqueta creation_date no definida -->]
	 * <!-- Etiqueta function_description no definida -->
	 *
	 * @return void
	 */
     function index(){
		try{
        
        	//inicia function_index_code
            <!-- Etiqueta function_index_code no definida -->
        	//termina function_index_code
	
			$data->output = $output;
			$data->title = '<!-- Etiqueta title_section no definida -->';
			
			/* Se carga la vista del Controlador*/
			$this->load->view('<!-- Etiqueta view_section_file no definida -->', $output);
		}catch(Exception $e){
			  /* En caso de error, lo mostramos */
			  show_error($e->getMessage().' --- '.$e->getTraceAsString());
		}
     }
     

  	/**
     * Código generado automáticamente por CMS-FRAMEWORK
	 * Búsqueda por id: <!-- Etiqueta function_description no definida -->
	 *
	 * @return void
	 */
     function id($valueId){
		try{
        
        	//inicia function_id_code
            <!-- Etiqueta function_id_code no definida -->
        	//termina function_id_code
	
			$data->output = $output;
			$data->title = '<!-- Etiqueta title_section no definida -->';
			
			/* Se carga la vista del Controlador*/
			$this->load->view('<!-- Etiqueta view_section_file no definida -->', $output);
		}catch(Exception $e){
			  /* En caso de error, lo mostramos */
			  show_error($e->getMessage().' --- '.$e->getTraceAsString());
		}
     }
     
  	/**
     * Código generado automáticamente por CMS-FRAMEWORK
	 * Búsqueda por página de resultados
	 *
	 * @param	page		Página
	 * @return 	void
	 */
     function page($page){
		try{
        
        	//inicia function_page_code
            <!-- Etiqueta function_page_code no definida -->
        	//termina function_page_code
	
			$data->output = $output;
			$data->title = '<!-- Etiqueta title_section no definida -->';
			
			/* Se carga la vista del Controlador*/
			$this->load->view('<!-- Etiqueta view_section_file no definida -->', $output);
		}catch(Exception $e){
			  /* En caso de error, lo mostramos */
			  show_error($e->getMessage().' --- '.$e->getTraceAsString());
		}
     }
     
     
  	/**
     * Código generado automáticamente por CMS-FRAMEWORK
	 * Búsqueda por coincidencia exacta de campo
	 *
	 * @param 	field   	Campos par aplicar la búsqueda
	 * @param 	q			Criterio de búsqueda
	 * @param 	page		Página
	 * @return 	void		
	 */
     function find($field, $q, $page=1){
		try{
        
        	//inicia function_find_code
            <!-- Etiqueta function_find_code no definida -->
        	//termina function_find_code
	
			$data->output = $output;
			$data->title = '<!-- Etiqueta title_section no definida -->';
			
			/* Se carga la vista del Controlador*/
			$this->load->view('<!-- Etiqueta view_section_file no definida -->', $output);
		}catch(Exception $e){
			  /* En caso de error, lo mostramos */
			  show_error($e->getMessage().' --- '.$e->getTraceAsString());
		}
     }
     
     
  	/**
     * Código generado automáticamente por CMS-FRAMEWORK
	 * Búsqueda por coincidencia similar (uso de Like %q%
	 *
	 * @param 	field   	Campos par aplicar la búsqueda
	 * @param 	q			Criterio de búsqueda
	 * @param 	page		Página
	 * @return 	void		
	 */
     function like($field, $q, $page=1){
		try{
        
        	//inicia function_like_code
            <!-- Etiqueta function_like_code no definida -->
        	//termina function_like_code
	
			$data->output = $output;
			$data->title = '<!-- Etiqueta title_section no definida -->';
			
			/* Se carga la vista del Controlador*/
			$this->load->view('<!-- Etiqueta view_section_file no definida -->', $output);
		}catch(Exception $e){
			  /* En caso de error, lo mostramos */
			  show_error($e->getMessage().' --- '.$e->getTraceAsString());
		}
     }
     
     <!-- Etiqueta extra_functions no definida -->
     
}


/* End of file <!-- Etiqueta section_name no definida -->.php */
/* Location: ./application/controllers<!-- Etiqueta url_section no definida -->.php */
